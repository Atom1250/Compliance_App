# PR-050 — F10: SQLite Phase-Down and Final Validation

## Checklist
- [x] Updated operator docs to mark SQLite as transitional/test-only and Postgres as system-of-record.
- [x] Added cutover validation runbook with final Postgres flow checklist.
- [x] Added transition guard tests for docs and runtime expectations.
- [x] Executed live Postgres E2E harness command and validated completed report/evidence contracts.
- [x] Fixed Postgres migration/runtime blockers uncovered during live validation.

## Commands Run
- `make lint` -> pass
- `make test` -> pass (`209 passed, 3 skipped`)
- `.venv/bin/python scripts/run_postgres_e2e.py --database-url postgresql+psycopg://compliance:compliance@127.0.0.1:5432/compliance_app --work-dir outputs/postgres-e2e-live` -> pass (`terminal_status=completed`)
- `COMPLIANCE_APP_POSTGRES_TEST_URL=postgresql+psycopg://compliance:compliance@127.0.0.1:5432/compliance_app .venv/bin/python -m pytest tests/test_db_migrations_postgres.py tests/test_postgres_e2e_harness.py tests/test_backend_parity.py` -> pass (`1 passed, 3 skipped`)

## Notes
- Postgres validation surfaced two migration/runtime issues now fixed:
  - Alembic revision IDs longer than 32 chars caused Postgres migration failures.
  - `chunk.content_tsv` Postgres `tsvector` type conflicted with ORM writes; normalized to `text`.
